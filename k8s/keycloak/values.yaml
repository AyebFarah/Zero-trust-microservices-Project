auth:
  adminUser: admin
  adminPassword: "admin-zero-trust-2026"

service:
  type: ClusterIP
  port: 8080

replicaCount: 1

postgresql:
  enabled: true
  auth:
    password: "keycloak-db-2026"

resources:
  requests:
    memory: "512Mi"
    cpu: "250m"
  limits:
    memory: "1Gi"
    cpu: "500m"

# Désactive HTTPS pour le dev local (Istio gère le chiffrement)
production: false
proxy: edge
